{% extends "base.njk" %}

{% block head %}
  <title>Συνεργείο Αυτοκινήτων στο Μοσχάτο | Σπύρος Κοντάκης</title>
  <meta name="description" content="Συνεργείο αυτοκινήτων στο Μοσχάτο. Ο Σπύρος Κοντάκης προσφέρει επαγγελματικές υπηρεσίες συντήρησης και επισκευής αυτοκινήτων.">
{% endblock %}

{% block content %}
    <section class="site-hero">
      <div class="container">
        <div class="row">
          <div class="col-lg-10 mx-auto">
            <div class="site-hero-content text-center">
              <h1>
              <br>
                KONTAKIS AUTO SERVICE <br>
                Συνεργείο Αυτοκινήτων στο Μοσχάτο
              </h1>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section class="site-achieve" id="achieve">
      <div class="container">
        <div class="row">
          <div class="col-12">
            <ul class="site-achieve-list text-center">
              {% for point in achieve.points %}
              <li>{{ point }}</li>
              {% endfor %}
            </ul>
          </div>
        </div>
      </div>
    </section>

    <section class="site-project" id="project">
      <div class="container">
        <div class="row">
          <div class="col-12">
            <div class="section-title">
              <h2>{{portfolio.subTitle}}</h2>
              <p>{{portfolio.title}}</p>
            </div>
          </div>
          {% set sortedPortfolios = collections.portfolios | sort(attribute='data.order') %}
          {% for portfolio in sortedPortfolios %}
          <div class="col-lg-6 col-md-10 mx-auto">
            <div class="site-project-item">
              <div class="site-project-item-thumb">
                <img src="{{portfolio.data.img}}" alt="project-thumb">
              </div>
              <div class="site-project-item-content">
                <span>{{portfolio.data.subTitle}}</span>
                <h3>{{portfolio.data.title}}</h3>
                <a href="{{portfolio.data.page.url}}" class="read-more">{{portfolio.data.linkText}}</a>
              </div>
            </div>
          </div>
          {% endfor %}
          <div class="col-12 text-center text-lg-left">
            <a href="/{{portfolio.seeAllbtn.link}}" class="site-project-cta">{{portfolio.seeAllbtn.text}}</a>
          </div>
        </div>
      </div>
    </section>
{% endblock %}